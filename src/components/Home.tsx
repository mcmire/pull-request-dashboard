import * as React from 'react';

/**
 * The React component for the home (main) screen.
 *
 * @returns The JSX to render the component.
 */
export default function Home() {
  return <p>Hello World</p>;
}
